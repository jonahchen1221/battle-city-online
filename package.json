{
  "name": "battle-city-online",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "tsx watch src/server/server.ts",
    "server": "tsx src/server/server.ts",
    "start": "tsx src/server/server.ts",
    "lan": "npm run build && tsx src/server/server.ts",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tsx": "^4.23.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "typescript": "^5.5.4",
    "vite": "^6.0.0"
  }
}
